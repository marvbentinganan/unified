<div class="ui {{ $modifiers }} input">
    <div class="ui label">
        {{ $label }}
    </div>
    <input type="{{ $type }}" name="{{ $name }}" id="{{ $id }}" placeholder="{{ $placeholder }}">
</div>
